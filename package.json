{
  "name": "zomato-reels",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun install --cwd backend && bun install --cwd frontend && cd frontend && bun run build",
    "start": "cd backend && bun run start"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
